# JWT_SECRET=change-me-use-a-long-random-string-in-production
JWT_SECRET=change-me-use-a-long-random-string-in-production
PORT=3001
NODE_ENV=development
DATABASE_PATH=./data/rihla.db
CORS_ORIGIN=http://localhost:5173

# Production — single origin or comma-separated list
# CORS_ORIGIN=https://rihla.example.com

# Optional SMTP (devis par email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="Rihla <noreply@rihla.ma>"

# Recherche voyage — Phase API (optionnel, mode live)
# DUFFEL_ACCESS_TOKEN=
# HOTELBEDS_API_KEY=
# MOZIO_API_KEY=
# VIATOR_API_KEY=
